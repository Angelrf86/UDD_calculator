# UDD_calculator

This repo contains code for simulating ultrafast dynamical diffration. The
original MATLAB implementation is in the `Matlab/` directory, and a Julia port
that can run on CPUs/GPUs is in `MultiBeamCalculator/`.
